using UnityEngine;

/// <summary>
/// Static obstacle — no logic needed beyond a collider.
/// Attach this as a marker component for clarity.
/// </summary>
public sealed class Obstacle : MonoBehaviour { }